import type { Options } from '../../../../types';

export declare function TNegInt32(value: unknown, options?: Options): value is number;
export declare function isNegInt32(value: number, options?: Options): boolean;
