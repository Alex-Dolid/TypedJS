import type { Options } from '../../../types';

export declare function TInt32(value: unknown, options?: Options): value is number;
export declare function isInt32(value: number, options?: Options): boolean;
