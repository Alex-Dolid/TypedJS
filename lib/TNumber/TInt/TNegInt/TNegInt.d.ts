import type { Options } from '../../../types';

export declare function TNegInt(value: unknown, options?: Options): value is number;
export declare function isNegInt(value: number, options?: Options): boolean;
