import type { Options } from '../../../types';

export declare function TPosInt(value: unknown, options?: Options): number;
export declare function isPosInt(value: number, options?: Options): boolean;
