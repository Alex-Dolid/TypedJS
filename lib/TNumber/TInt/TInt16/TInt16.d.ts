import type { Options } from '../../../types';

export declare function TInt16(value: unknown, options?: Options): value is number;
export declare function isInt16(value: number, options?: Options): boolean;
