import type { Options } from '../../../types';

export declare function TFloat32(value: unknown, options?: Options): value is number;
export declare function isFloat32(value: number, options?: Options): boolean;
