import { BaseTJS } from './BaseTJS';

describe('BaseTJS', () => {
  let baseTJS;

  describe('BaseTJS [strict=false]', () => {

    beforeEach(() => {
      baseTJS = new BaseTJS();
    });

    describe('[Method] getType', () => {
      [['[Number]: should return number']].forEach(() => {
        it()
      })
    })
  })
})

